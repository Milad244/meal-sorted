import type { MealProps, NutritionProps, PriceProps } from "@/components/MealCard";

type MealFormProps = {
    meal?: MealProps;
    nutrition?: NutritionProps;
    price?: PriceProps;
};

export default function MealForm({ meal, nutrition, price }: MealFormProps) {
    return (
        <div>
            
        </div>
    );
}
